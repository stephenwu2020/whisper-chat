export class Private {
  public id: string;

  public name: string;

  public pubKey: string;

  constructor(id: string, name: string, pubKey: string) {
    this.id = id;
    this.name = name;
    this.pubKey = pubKey;
  }
}
